#include <stdio.h>
/**
 * main - Entry point
 * Description: A program that prints all single digit up to 10
 * Return: 0
 */
int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
	{
		printf("%d", a);
	}
	printf("\n");
	return (0);
}
