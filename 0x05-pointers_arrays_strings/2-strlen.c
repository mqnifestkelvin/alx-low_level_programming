#include "main.h"
/**
 * _strlen - This function checks the length of a string.
 *
 * @s: This accepts a string value 
 *
 * Return: 0
 *
 */
int _strlen(char *s)
{
	char string[] = s;
	int i,

	for(i = 0; c[i] != '\0'; i++);
	return (i);
}
